Proxima: Async reverse HTTP proxy
=================================

Proxima is an asynchronous reverse HTTP proxy for Python 3 built on
aiohttp.

It's currently highly experimental. It also needs an unreleased version
of aiohttp to run the tests.

